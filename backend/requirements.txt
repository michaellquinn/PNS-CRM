fastapi>=0.115
uvicorn[standard]>=0.32
asyncmy>=0.2.9          # OceanBase / MySQL-wire driver (NOT asyncpg/Postgres)
python-multipart>=0.0.9 # required for multipart/form-data — FastAPI file uploads 400 without it
